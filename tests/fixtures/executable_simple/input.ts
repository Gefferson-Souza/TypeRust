// Executable Test - Pure functions without external dependencies

function add(a: number, b: number): number {
    return a + b;
}

function multiply(x: number, y: number): number {
    return x * y;
}

function calculateTotal2(a: number, b: number, c: number): number {
    return add(a, b) + c;
}
